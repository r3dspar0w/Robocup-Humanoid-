Place the deployed locomotion policy here as `policy.onnx`.

The brain launch file will use this packaged model by default unless
`custom_walk_model_path:=/abs/path/to/policy.onnx` is provided at launch time.
